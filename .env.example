# Core DNA v2 — Environment Configuration

# ========================================
# SUPABASE CONFIGURATION (Required)
# ========================================

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ========================================
# n8n AUTOMATION ENGINE (Optional)
# ========================================

# n8n Automation (Optional - for workflow features)
VITE_N8N_API_URL=http://localhost:5678/api/v1

# ========================================
# APPLICATION SETTINGS
# ========================================

# App Environment
VITE_APP_ENV=development

# Enable Debug Mode
VITE_DEBUG_MODE=false

# ========================================
# API KEYS - MANAGED BY USERS (BYOK MODEL)
# ========================================

# ✅ Users add their own API keys in Settings (not in .env)
# ✅ Keys are stored in browser localStorage only
# ✅ CoreDNA never sees or stores your API keys
# 
# Supported: 70+ providers across LLM, Image, Voice, Automation
# See Settings → API Keys for full list
#

# ========================================
# NOTES
# ========================================
#
# 1. Copy this file to .env.local and fill in REQUIRED fields only
# 2. Never commit .env.local to version control
# 3. Only variables prefixed with VITE_ are exposed to the client
# 4. API keys are NOT stored in .env - users add them in Settings
# 5. n8n must be running locally (http://localhost:5678) or point to your hosted instance
# 6. All workflow automation runs silently — users don't see n8n unless they're in Automations (Hunter tier)
# 7. Core DNA is fully white-labeled — no external branding exposed
#
